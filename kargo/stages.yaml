apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: dev
  namespace: traces-test
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: traces-test-warehouse
      sources:
        direct: true
  promotionTemplate:
    spec:
      vars:
        - name: gitopsRepo
          value: https://github.com/tijnsemmekrot/traces-test.git
        - name: branch
          value: kargo-implementation
        - name: registry
          value: 000000000000.dkr.ecr.eu-west-1.localhost.localstack.cloud:4566
      steps:
        - uses: git-clone
          config:
            repoURL: ${{ vars.gitopsRepo }}
            checkout:
              - branch: ${{ vars.branch }}
                path: repo

        # Service 1: account-service (Index 0 in Freight)
        - uses: kustomize-set-image
          as: update-account-service
          config:
            path: repo/environments/dev/account-service
            images:
              - image: ${{ vars.registry }}/traces-test-account-service
                # Trying the ctx.warehouse naming convention for image access
                newTag: ${{ ctx.warehouse('traces-test-warehouse').images[0].tag }}

        # Service 2: fraud-detector (Index 1 in Freight)
        - uses: kustomize-set-image
          as: update-fraud-detector
          config:
            path: repo/environments/dev/fraud-detector
            images:
              - image: ${{ vars.registry }}/traces-test-fraud-detector
                newTag: ${{ ctx.warehouse('traces-test-warehouse').images[1].tag }}

        # Service 3: notification-service (Index 2 in Freight)
        - uses: kustomize-set-image
          as: update-notification-service
          config:
            path: repo/environments/dev/notification-service
            images:
              - image: ${{ vars.registry }}/traces-test-notification-service
                newTag: ${{ ctx.warehouse('traces-test-warehouse').images[2].tag }}

        - uses: git-commit
          as: commit
          config:
            path: repo
            message: |
              Promote to dev
              ${{ outputs['update-account-service'].commitMessage }}

        - uses: git-push
          config:
            path: repo
            branch: ${{ vars.branch }}
